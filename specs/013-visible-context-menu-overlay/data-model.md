# Data Model: Visible Context-Menu Overlay

## Data Changes

No new data structures or persistent files are introduced.

| Aspect | Current | After change |
|--------|---------|--------------|
| New files | none | none |
| Modified files | `src/impactite/app.py` (CSS only) | same |
| Configuration keys | none | none |

The change removes a solid full-screen overlay background and sizes the context menu container to its content.
